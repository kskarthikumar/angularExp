import { NewsDescDirective } from './news-desc.directive';

describe('NewsDescDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsDescDirective();
    expect(directive).toBeTruthy();
  });
});
